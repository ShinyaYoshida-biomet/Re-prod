{
	"name": "re-prod",
	"version": "0.1.0",
	"private": true,
	"description": "AI-Powered R Analysis IDE",
	"scripts": {
		"dev": "cd ui && trunk serve --port 5176",
		"dev:server": "RUST_LOG=info cargo run -p reprod-server",
		"dev:tauri": "cargo tauri dev",
		"build": "cd ui && trunk build --release",
		"test": "cargo test",
		"loc": "tokei"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2.9.1"
	}
}
