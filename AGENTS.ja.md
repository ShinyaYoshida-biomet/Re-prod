# AI エージェント向け指示書

このドキュメントには、Re-prod コードベースで作業する AI コーディングエージェント向けの指示が含まれています。

**貢献ガイドライン、セットアップ手順、開発ワークフローについては、[CONTRIBUTING.md](./CONTRIBUTING.md) をご覧ください。**

## 基本原則

あなたは Re-prod プロジェクトのコード品質を維持する責任を持つ AI エージェントです。プロジェクトが成長するにつれて、コード品質と保守性が最も重要です。

## SOLID 原則

Robert C. Martin (Uncle Bob) が述べた SOLID 原則に従います:

### SRP) 単一責任の原則

> 同じ理由で変更されるものをまとめる。異なる理由で変更されるものを分離する。

### OCP) 開放閉鎖の原則

> モジュールは拡張に対して開いており、変更に対して閉じているべきである。

ただし、過度なエンジニアリングは避けてください。厳密な遵守が不必要な複雑さにつながる場合は、実用主義を優先すべきです。

### LSP) リスコフの置換原則

> インターフェースを使用するプログラムは、そのインターフェースの実装によって混乱してはならない。

### ISP) インターフェース分離の原則

> インターフェースを小さく保ち、ユーザーが必要としないものに依存しないようにする。

### DIP) 依存性逆転の原則

> 抽象の方向に依存する。高レベルのモジュールは低レベルの詳細に依存すべきではない。

## テスト駆動開発

ユニットテストは、各ユニットの低レベル仕様(契約)として機能します: 特定の入力/前提条件が与えられた場合、ユニットは定義された方法で動作しなければなりません(出力、副作用、不変条件)。

Kent Beck と和田卓人が提唱するテストファースト・アプローチに従ってください:
- 機能を実装する**前に**動作または受け入れ基準を定義する
- 最初に失敗するテストを書く(red)
- テストを通過させる最小限のコードを実装する(green)
- テストをグリーンに保ちながらリファクタリングする

## AI エージェントの開発ワークフロー

- `git add <files>` と `git commit` を使用して頻繁にコミットする
- 編集完了後、`.husky/` のすべての pre-push フックが通過することを確認する
- レビューを要求する前に、`.github/` で定義された CI/CD チェックが通過することを確認する

## 追加事項

`AGENTS.{username}.md` に開発者固有の AI エージェント指示が存在する場合、このファイルの後に読み込んで、これらのデフォルトを拡張またはオーバーライドしてください。
